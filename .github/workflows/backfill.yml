name: Backfill Julho (manual)

on:
  workflow_dispatch:   # só roda quando você clicar em "Run workflow", nunca sozinho

  jobs:
    rodar:
        runs-on: ubuntu-latest
            steps:
                  - uses: actions/checkout@v4

                        - uses: actions/setup-python@v5
                                with:
                                          python-version: "3.11"name: Backfill Julho (manual)

                                          on:
                                            workflow_dispatch:

                                            jobs:
                                              rodar:
                                                  runs-on: ubuntu-latest
                                                      steps:
                                                            - uses: actions/checkout@v4
                                                                  - uses: actions/setup-python@v5
                                                                          with:
                                                                                    python-version: "3.11"
                                                                                          - name: Instalar dependencias
                                                                                                  run: pip install -r requirements.txt
                                                                                                        - name: Rodar backfill de julho
                                                                                                                env:
                                                                                                                          KOMMO_TOKEN: ${{ secrets.KOMMO_TOKEN }}
                                                                                                                                    SHEET_ID: ${{ secrets.SHEET_ID }}
                                                                                                                                              GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
                                                                                                                                                      run: python backfill_julho.py

                                                - name: Instalar dependencias
                                                        run: pip install -r requirements.txt

                                                              - name: Rodar backfill de julho
                                                                      env:
                                                                                KOMMO_TOKEN: ${{ secrets.KOMMO_TOKEN }}
                                                                                          SHEET_ID: ${{ secrets.SHEET_ID }}
                                                                                                    GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
                                                                                                            run: python backfill_julho.py
